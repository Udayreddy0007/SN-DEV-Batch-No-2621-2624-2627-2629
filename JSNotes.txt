
---> Date : 16/09/26

What is JS ? and what it does to a page :
   -- its a programming language
   -- its both server side and client side language
   -- to manage the behaviour and tto write the business logic we  use java script.

    -- form validation , passwords checking , button events.
    -- used to make web page interactive 

    How js is used in SN ?

    -- To write the business logic and managing the behaviour , processing  the data and fetching the data 
        we gonna use JS in Sn we write scripts

        We have two consoles in SN
        
        -- Client side  : browser lvl
         CRUD / CRED
           Onload , on change , on submit , on celledit
 
        -- Server side : DB lvl
          DML operations
          Glide API's
          Glide methods
          Glide objects
          Glide Ajax
          Business rules
          Script includes


---> Date: 17/9/26
     
     -->Features :

        -- lighweight code
        -- it is platform independent language 
        -- it supports Object-oriented language 
        -- event-driven -buttonevents , mouse events
        -- it run on both server side and client side

    -- Scripts , logic , business logics

 Js - it can be written inside html and out side html 
  
  there are two way to write JS
  
  1.internal JS : inside the html body we gonna write the logic by using <script></script>
  2.External JS : we create a seperate file and it should be stored with .js extention
                  and linked with html by using script tag inside the body

  --> What is data ?
       
       --- which contains information 
           name  , channel ids, age , email
        
  --> which type of data it is ?

  name : uday - characters / alphabets
  phnno: 9848512199 - numbers / numeric digits 

  Data types 

  --> every data as information about something and the data will be in different format / either it may be in the digits , alphabets ,true
      false like this

    5 data types in JS :

  1. int(integer data type) - the information/data is in the form of whole numbers
    
       ex : phnno: 952535192;
  
  2. decimal: if a number as more than 2 percision values we consider that as decimal data types if the 
               number length is more than 15 by default js consider it as decimal datatype
      ex: percentage : 99.98%
          Total bill : 1025.00345637389;

  3.Float : if a number as 1 percision value we consider it as float data type the  length is  5 to 7
      
      ex: population : 144.1;  

  4. String :  group of letters,characters , combination of letters and numbers and the data should be decalred it btw
              double quotes ("stringdata")
   
   ex :  name : uday ;
               "uday";
         id : "21M91A0511"
  
  5. Boolean (True/false ): it will check the conditions 
       
       ex: he/she is 18 and eligible to vote -- true
           he/she is 16 and major -- false
      
      -- computerunderstanding only the machine langauge (0 and 1)
      -- 0 is treated as true 
      -- 1 is treated as false
      -- yes / no
  
  ---> output statement : the statement / code which is written to diaplay the output
    
     console.log();

     console.log(name);
     
   
    -- when we move on the SN platform there the output is different 
     
      gs.info / gs.print


---> Date : 18/9/26

   --> Variable : it is a container which stores different types of data and that different types of 
                  data is stored inside a name that name we call it as variable name

                  marks = 90;
                  marks -- variable name / container 
                  90 -- is the int type data

    varaible naming rules in JS

      -- a variable name can starts with _ or $ or character but not numbers
        
            ex : id = "2134A";
                 1id ="2134A";
                 _id="2134A";
                 $id="2134A";
                 #id ="2134A";
    
    -- a variable name cannot be a default names 
      
      ex : for , if , count ,...... etc

    --- JS uses camelCase naming convention for writing a variable names
           
           ex : stdName ="Shubash";
   
    -- variable name cannot contains any spaces
      
        ex : std_Name 
             std Name 
             s tdName
             StdName

  
  ---> Variable keywords :
   
       -- In JS we have three  variable keywords
         
         1.let -- it is a block scope variable  the value can be updated 
                    but we cannot redecalre the variable
         
         2.var -- it is a global scope variable the value can be updated and redecalration

         3.const -- it is a block scope variable the values cannot be updated and redecalred


   -- JS is a case sensitive language 
      
      ex : name & Name  -- same meaning , spelling but act differently

       -- Syntax : 
                   varaible keyword variableName = value / type of the data / data ;


--> Operators : Operators are used to perform calculations , to check the conditions, compare the things we gonna use Operators

     There are  operators inside the JS 

     1.Arithmatic : for maths calculations
        (+ , - , / , % ,* ) --
           
          --> +  :  var a = 15;
                b = 16; //  output will be 31
                result= a + b ;
               console.log("The addition of two :"+result);


          --> - :   var a = 15;
                   b = 16; //  output will be -1
                   result= a - b ;
                  console.log("The Substraction  of two :"+result);

         -->  / : 
                 var a = 15;
                b = 16; //  output will be 0.93 
                result= a / b ;
               console.log("The Division of two :"+result);

         --> % : 
                     var a = 5 ;
                     b = 10; //  output will be 5 
                     result= a + b ;
                     console.log("The modulo of two :"+result);

        --> * : 
                     var a = 15;
                    b = 16; //  output will be 240
                    result= a + b ;
                    console.log("The Multiplication  of two :"+result);
                     
    
    2.Compound Assignment / Assignment operators - this operators are the combinations of = and Arithmatic

         (+= , -=, /= ,%=,*=)

      --> 
                var a = 15;
                a+=2; // 17  
                a-=3; // 14
                a*=4; //56 
                a /=4; //14
                a %= 7; // 0
                console.log(a);

    --> Ternary Operators --- **

      ** -- i want to find  2 square , cube, quadartic .............
         
          2 ** 2 ;
          3 ** 4 ;
          4 ** 5 ;


    3.Comparison Operators - To compare the values we gonna use this Operators
       (<, >, <=, >=, ==, === , !=) - true / false
        
        == : 
              is used to compare two values 
               
         var a = 10 ;
         b = 10;
         c = a == b; // 10 == 10 // True
         // 10 == 20// false

     === :
         
         a = 10;
         b="10"; 
         compare the value also check the type of Data
          // false
          exact Comparison
          a === b

    != : 
          a= 10 ;
          b = 20;
           a != b; // true





    4.Logical operators - To check the conditions output in the form as true or false

       AND (&&) -- we have conditions 
                                 cond1(He/she age is 18 and we can treat them ar majaor) = true;
                                 cond2 (he/she is eligible to vote) = true;
                                 if and only if both cond are true then only get output as True
      
       OR(||) -- if the fst or the second is true then get output as true
       
       NOT(!) -- reverse  
           cond1 = true; -- he /she age is 18 and we can treat them major
          console.log(!cond1);

    5.increment/decrement - (++ , --)  --- 

       increment -- it add +1 to the original value
             var a = 10 ;
              a++ // -- a + 1 =10 +1 = 11
      
       decrement --     it subtract -1 from the original value

          var b = 20;
            b--; // -- b - 1 = 20-1 = 19

--> Date: 22/9/26

  --- we have seen examples on Comparison , logical & Inc , dec operators


  --- Control Statements / Conditional statements :
    
      A block of code is executed when the conditions are matched / true -- block of code

      {
           // block code // console.log()
      }

      there are few types of ctrl stmts or cond stmts in JS they are

      1. if conditional statement  --
        
        syntax/ structure :
                     
                     if(cond){
                         // true statement executed if the condition is true // true block
                     }
      2. if else statement --
           syntax / structure :
             
              if(cond){
                   //true block
              }
              else{
                   // false block / default block
              }

      3. else if statement : 
              
              syntax/structure: 

                 if(cond1){
                    //true block
                 }
                 else if(cond2){
                    // false block
                 }
                 else{
                     // default block
                 }
           
      4 . nested else if : 
                    we can write multiple else if

                    syntax/structure:

                    if(cond1)
                    {
                         //true block 
                    }
                    else if(cond2)
                    {
                         // false block
                    }
                    else if(cond3){
                         // false block
                    }
                    else if(cond4){
                         // false block
                    }
                    else{
                         // default block
                    }

      5.nested if : 
       
--> Date : 23/9/26

  5. nested if :
                
  syntax :
              if(cond1)
                  {
                          // stmt 1 if it is true executed
                 }
           if(cond2)
           {
               // stmt 2 if it is true executed
           }
           if(cond3)
           {

           }

  6.Switch statement :  The case is executed when the condition is true
   
   syntax :
                 switch(expression){
                    case1 : 
                             Console.log();
                    break ;       ------------- break will tell the case to stop here if the case 1 is true
                    case 2 :
                            console.log();
                            break;
                    Case 3 :
                            console.log();
                            break;

                    default:
                            console.log();
                 }

 --> Loop control statements / iterative control statements --
        
        A loop will iterate multiple times until the conditions

        ex :  var name = "Chandhana";
             console.log(name);
             console.log(name);
             console.log(name);
             console.log(name);
             cosnole.log(name);

 1. for loop  -- the loop is iterated multiple time until the stopping condition
   
     syntax : 
                for(var i=0; // intial value i<=5; // stopping condt i++){

                    // output statement
                }

          var name = "Rajesh";
          for(var i=1; i<=10; i++) -- i = 1 -- 1<=10 -- true -- i++ -- 1+1 =2
          {
               console.log(name);
          }


 2.while loop 

 3. do while

 4 . for each loop

 ---> Date : 24 - 09 -26

     -- While loop : the loop will exectue when the condition is True

     syntax :  while(stoping condition i<=) -- condition
               {
                    console.log();
                    incremented (i++);
               }

     ---  Note : one record we can go with if condition  , to update multiple in sn  we use while loop


     --- Do while loop : the loop execute at least once and then condition 

         syntax : do {
                        console.log();
                   }while(cond);


                   ex :  var a = 3 ;
                         i=1;
                         do{
                             
                             console.log(a);
                             i++;
                         }while(i<=10);
                    
                         
      --- Note : dont define i value inside the Loop{}


---> Notification Keyword -- Notification remainder /  a popup on a webbrowser/page
   
   -- There are three types of notification keywords 
    
     1.alert -- a dialog box with some msg is populated over the page
        
        syntax : alert('We are about to enter into intermediate level');

     2.confirm :  a dialog box with some msg is populated over the page
         
         syntax :  confirm("hello");
     
     3.prompt : a dialog box which allow the user to enter the text 
        
        syntax : prompt ("enter your name");


---> Array -- group of related / collection of same information

   ex : fruits = ["apple","pineapple","watermelon"];
          fruits.length = 3
      
      ---  indexing each fruit associated with value which it location
                  the indexing starts from 0
                  console.log(fruits[1]);
                  console.log(fruits.length);

---->Date : 25/9/26

 --> How to find the index of each data that is stored inside an array


 var states = ["TS","MH","KN","TN","AP","DE","SK","JK","JM","MP","UP","GA","GJ","BH","RJ","WB"];
 
 for(var i=0; i<states.length; i++){
     console.log(index + " : " + value);
 }


  ex :  var states = ["TS","MH","KN","TN","AP","DE","SK","JK","JM","MP","UP","GA","GJ","BH","RJ","WB"];
 
         for(var i=0; i<states.length; i++){
          console.log("Index : "  + i + "value" + states[i]);
          }


--> For-each :  

            syntax of for is  : 

               for (var variablename of variablenames){ --- state of states 
                    console.log(variableName);
               }

 ---> Example : for each Loop


     var states = ["TS","MH","KN","TN","AP","DE","SK","JK","JM","MP","UP","GA","GJ","BH","RJ","WB"];
 
     for(var state of states)
     {
     console.log("The state code is : " + state);
      }


--> Functions in JS :  A block of reusable code 

   syntax : function functionName (parameters names){
                       
                       return ; // it gives inn return something
   
                }
                functionName(values of the parameter)

--> 1.function without parameters : 

       function funName(){
          console.log("hello");
       }
       funName(); // function calling


       ex : 
                  function welcome()
                      {
                               console.log("Welcome to functions in js");
                      }
                           welcome();

     

    2.function with parameters   :

                  function funname(name){
                    console.log("The name is : " + name)
                  }  
                  funnname("uday");


                  ex : 
                       function add(a,b){
                           return a+b; //does some action
                       }
                       add(10,20); 
    
     3.function with multiple parameters :

        ex :
                function add(a,b,c){
                    return a+b+c; //does some action
                }
                add(10,20,30);


          ex : 
                     function names(nam1 , nam2, nam3){
                         return [nam1,nam2,nam3];
                     }
                     names("uday","chandhana","shubhash");
                
  
   
    
     
            




         





